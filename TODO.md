# TODO: Fix ChatContainer.jsx Errors

- [ ] Update imports: Add useState and useContext to React import, remove server controller imports, add toast import
- [ ] Fix 'cons' to 'const' for input state
- [ ] Rename first function from 'handleSendessage' to 'handleSendMessage'
- [ ] Rename second function to 'handleSendImage' and fix arrow function syntax
- [ ] Fix 'iage' to 'image' in sendMessage call
- [ ] Fix JSX: Add '?' to onlineUsers.includes(selectedUser._id), correct img src, fix 'assests' to 'assets'
- [ ] Change onChange={handleMessage} to onChange={handleSendImage}
- [ ] Fix selectedUser_id to selectedUser._id in useEffect and onlineUsers check
- [ ] Verify all references and run app to test
